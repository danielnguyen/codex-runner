# Optional integrations

Integrations are optional client adapters that consume the documented codex-runner HTTP API. The runner core never imports or depends on them. Each integration keeps its client-specific setup, dependencies, tests, and security guidance in its own directory.

- [Open WebUI Pipe](openwebui/README.md)
